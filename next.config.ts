/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true, // Habilita el modo estricto de React para encontrar problemas potenciales en tu código
    swcMinify: true,       // Usa el compilador SWC de Next.js para minificar el código
  };
  
  module.exports = nextConfig;